<Window x:Class="DataGridDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataGridDemo"
        mc:Ignorable="d"
        Title="ErrorCellNavigation" Height="450" Width="800">
    
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <syncfusion:SfDataGrid x:Name="dataGrid"
                               ItemsSource="{Binding Orders}"
                               GridValidationMode="InView"
                               AllowEditing="True"
                               SelectionUnit="Cell"
                               AutoGenerateColumns="False">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="Customers.CustomerName" 
                                           HeaderText="Customer Name"
                                           Width="200"/>
                <syncfusion:GridTextColumn MappingName="ShippersInfo[0].ShipperID"
                                           HeaderText="ShippersInfo[0].ShipperID"
                                           Width="200"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>

        <Button Grid.Row="1" Content="MoveToErrorGridCell" Click="OnMoveToErrorGridCell" Height="30" Width="250"/>
    </Grid>
</Window>
